---
name: harness:naming-craft
description: LLM-judgment skill that critiques identifier names (variables, functions, types, files) against a curated rubric catalog seeded from Martin / Beck / Karlton. First craft-pipeline ceiling skill; cross-cutting (other craft skills call into it for domain-specific naming).
argument-hint: '[--path <path>] [--files <files>] [--kinds <kinds>]'
allowed-tools:
  - Bash
  - Read
  - Glob
  - Grep
---

<!-- Generated by harness generate-slash-commands. Do not edit. -->

<context>
Cognitive mode: constructive-architect
Type: rigid
</context>

<objective>
LLM-judgment skill that critiques identifier names (variables, functions, types, files) against a curated rubric catalog seeded from Martin / Beck / Karlton. First craft-pipeline ceiling skill; cross-cutting (other craft skills call into it for domain-specific naming).

Phases:

- extract: TS Compiler API walk; gather identifiers + scope metadata + context lines
- sample: Derive project naming convention via majority-rule sampling
- critique: LLM-rubric loop per (identifier, rubric) pair; emit 3-axis findings
- report: Aggregate findings + LLM cost + convention summary
  </objective>

<execution_context>
@agents/skills/claude-code/naming-craft/SKILL.md
@agents/skills/claude-code/naming-craft/skill.yaml
</execution_context>

<process>
1. Try: invoke mcp__harness__naming_craft with skill: "naming-craft"
2. If MCP unavailable: read SKILL.md and follow its workflow directly
3. Pass through any arguments provided by the user
</process>
