---
name: harness:copy-craft
description: LLM-judgment critique of prose-in-code across six surfaces (error messages, log lines, CLI output, commit subjects, PR descriptions, code comments). Third craft-pipeline ceiling skill; primary domain is error messages (universally bad). Graceful degradation when git/gh prerequisites are absent.
argument-hint: '[--path <path>] [--files <files>] [--surfaces <surfaces>] [--commits-since <commits-since>]'
allowed-tools:
  - Bash
  - Read
  - Glob
  - Grep
---

<!-- Generated by harness generate-slash-commands. Do not edit. -->

<context>
Cognitive mode: constructive-architect
Type: rigid
</context>

<objective>
LLM-judgment critique of prose-in-code across six surfaces (error messages, log lines, CLI output, commit subjects, PR descriptions, code comments). Third craft-pipeline ceiling skill; primary domain is error messages (universally bad). Graceful degradation when git/gh prerequisites are absent.

Phases:

- extract: TS Compiler API for source surfaces; git/gh shell-out for commits + PRs
- critique: LLM-rubric loop per (item, rubric) where rubric applies to that surface
- report: Aggregate findings + per-surface counts + cost telemetry + skipped surfaces
  </objective>

<execution_context>
@agents/skills/claude-code/copy-craft/SKILL.md
@agents/skills/claude-code/copy-craft/skill.yaml
</execution_context>

<process>
1. Try: invoke mcp__harness__copy_craft with skill: "copy-craft"
2. If MCP unavailable: read SKILL.md and follow its workflow directly
3. Pass through any arguments provided by the user
</process>
