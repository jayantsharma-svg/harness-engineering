---
name: harness:detect-design-drift
description: Detect design-system drift — hardcoded values where tokens exist and raw HTML primitives where registered components exist. Reports only; never modifies source. Floor-layer rule-based verifier composed by harness check-design.
argument-hint: '[--path <path>] [--mode <mode>] [--files <files>]'
allowed-tools:
  - Bash
  - Read
  - Glob
  - Grep
---

<!-- Generated by harness generate-slash-commands. Do not edit. -->

<context>
Cognitive mode: constructive-architect
Type: rigid
</context>

<objective>
Detect design-system drift — hardcoded values where tokens exist and raw HTML primitives where registered components exist. Reports only; never modifies source. Floor-layer rule-based verifier composed by harness check-design.

Phases:

- scan: Load tokens.json and DESIGN.md registry; walk candidate files
- apply_rules: Run DRIFT-T* and DRIFT-P* rules per file
- report: Aggregate severity, format findings, persist to graph
  </objective>

<execution_context>
@agents/skills/claude-code/detect-design-drift/SKILL.md
@agents/skills/claude-code/detect-design-drift/skill.yaml
</execution_context>

<process>
1. Try: invoke mcp__harness__run_skill with skill: "detect-design-drift"
2. If MCP unavailable: read SKILL.md and follow its workflow directly
3. Pass through any arguments provided by the user
</process>
