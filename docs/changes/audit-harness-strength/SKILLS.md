# Skill Recommendations — audit-harness-strength

> Generated manually (advise_skills MCP tool unavailable this session). Re-run
> `advise_skills` when MCP is restored to refresh.

## Apply

- **harness:skill-authoring** — writing the new `SKILL.md` + `skill.yaml`
  (Phase 4) in the rich rigid-skill format.
- **harness:tdd** — each of the 7 rules ships with a passing/failing fixture
  pair; rule detection is the textbook TDD case (Phase 2).
- **harness:planning** — immediate next step: break this spec into the 5-phase
  execution plan.

## Reference

- **harness:security-scan** — the structural model to mirror (SCAN→FILTER→
  REPORT, core engine + `check-security` command, severity gate).
- **harness:detect-design-drift** — absent-input silent-skip pattern and
  rule-family organization.
- **harness:enforce-architecture** — `architecture.thresholds` domain knowledge
  for STRENGTH-004.

## Consider

- **harness:soundness-review** — re-run against the plan once it exists.
- **harness:code-review** — multi-persona review of the core engine before
  ship, given it gates CI.
