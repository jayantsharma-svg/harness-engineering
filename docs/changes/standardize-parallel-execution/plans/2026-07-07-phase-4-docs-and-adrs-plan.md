# Plan: Standardize Parallel Execution — Phase 4 (Docs + ADRs)

**Date:** 2026-07-07 | **Spec:** `docs/changes/standardize-parallel-execution/proposal.md` | **Tasks:** 7 | **Time:** ~28 min | **Integration Tier:** medium

## Goal

Document the risk-tiered non-blocking dispatch policy and the `dependsOn` plan-task contract as durable ADRs, declare parallel execution a standard/automatic part of the build loop in AGENTS.md, wire the doc cross-references, and record the new graph concepts — closing out the feature with a reviewable paper trail. **Docs-only: no source, no tests, no SKILL.md edits.**

## Context (Phases 1-3 are complete — what already exists)

Read the current state before editing; do not restate or re-decide it.

- **Core module** `packages/core/src/parallelization/plan.ts` exports `planParallelization`, `classifyFiring`/`deriveFiring`, `validatePlanTasks`, `buildTaskGraph`, `waveSeverity`, `narrate`, and the `ParallelizationPlan` / `ParallelizationWave` / `FiringDecision` types. This is the authoritative source for the ADR prose — cite it, do not paraphrase loosely.
- **MCP tool** `plan_parallelization` (`packages/cli/src/mcp/tools/parallelization.ts`) is registered and **already documented** in `docs/reference/mcp-tools.md` (line ~615, added in Phase 3). That file carries an `AUTO-GENERATED — do not edit` banner; it is regenerated by `pnpm run generate-docs`. **Phase 4 must not hand-edit it.**
- **Skill prose** (Phase 3) already wires the pre-dispatch step into `harness-autopilot` EXECUTE (SKILL.md line ~111), standalone `harness-execution` Phase 2, `harness-planning` SEQUENCE step 2 (records `dependsOn`), and `harness-parallel-agents` (autopilot-invoked mode). **Phase 4 edits NO SKILL.md files** — all four-platform skill wiring is done and verified byte-identical (Phase 3 handoff).
- **Firing policy as implemented** (`classifyFiring`, plan.ts:180-217, and the cross-bucket cap at plan.ts:362-377) — the ground truth the ADR documents:
  - `high` severity → `serialize`
  - wave size `< minWaveSize` (default 3) → `serialize`
  - `medium` severity → `confirm`
  - `analysisLevel === 'file-only'` (graph unavailable) → `confirm`
  - `none` **or** `low` severity + `graph-expanded` → `auto-dispatch`
  - **cross-bucket cap (P2-IMP-1):** an otherwise-`auto-dispatch` wave whose direct upstream runs in the `serialized`/`cyclic` channel is capped to `confirm` (never weakened, never forced all the way to `serialize`).
- **Validation as implemented** (`validatePlanTasks`, plan.ts:113-143): unknown `dependsOn` id → hard error; dependency cycle (over the combined graph = explicit `dependsOn` ∪ file/`owns` overlap) → hard error; a task depending on a later-declared task (consumer before producer) → warning.

## Observable Truths (Acceptance Criteria)

1. `docs/knowledge/decisions/0056-risk-tiered-non-blocking-dispatch.md` exists with valid ADR frontmatter (`number: 0056`, `status: accepted`, `source:` the proposal) and documents: the clean→auto-dispatch / medium+graph-unavailable→confirm / high→serialize tiers; the cross-bucket cap; the announce-and-proceed philosophy; **and explicitly that `low`-severity + `graph-expanded` → `auto-dispatch` is an intentional extension of the spec's enumerated cases (Phase-2 review suggestion SUG-3, deferred to this ADR).**
2. `docs/knowledge/decisions/0057-depends-on-plan-task-schema.md` exists with valid ADR frontmatter (`number: 0057`, `status: accepted`) and documents the `PlanTask.dependsOn` contract, its relationship to roadmapped `owns:[paths]` (#601, consumed-if-present but not owned here), and the cycle / unknown-id validation rules.
3. `AGENTS.md` contains a new section stating parallel execution is now a standard/automatic part of the build loop (harness derives the DAG and dispatches sound parallelism without being asked), pointing at the skills (`harness-autopilot`, `harness-planning`, `harness-parallel-agents`) and the `plan_parallelization` MCP tool.
4. `docs/guides/agent-worktree-patterns.md` cross-references the new auto-dispatch path (the guide is now cited BY autopilot, and points back at it).
5. `docs/knowledge/core/parallelization-plan.md` records the three graph concepts (`parallelization-plan`, `task-dependency-dag`, `risk-tiered-dispatch`) and the `harness-autopilot` —consumes→ `plan_parallelization` —wraps→ `findParallelGroups` + `predict_conflicts` relationship, using the lightweight `business_concept` knowledge convention.
6. Every new doc (both ADRs + the knowledge concept doc) has at least one inbound link **by basename** from another `docs/` markdown file (link-based doc-coverage: ADRs/knowledge doc are cross-linked from AGENTS.md and each other; the knowledge doc is linked from both ADRs' "See also").
7. `pnpm format:check` passes on all new/edited markdown; `pnpm generate-docs --check` exits 0 (mcp-tools.md unchanged, no drift); `pnpm generate:plugin:check` exits 0 (no skill drift — Phase 4 touched no SKILL.md); `harness validate` == pre-existing baseline (390 issues, no NEW findings).
8. `docs/reference/mcp-tools.md` still contains the `plan_parallelization` entry (verification only — not edited; it is auto-generated).

## File Map

- CREATE `docs/knowledge/decisions/0056-risk-tiered-non-blocking-dispatch.md`
- CREATE `docs/knowledge/decisions/0057-depends-on-plan-task-schema.md`
- CREATE `docs/knowledge/core/parallelization-plan.md`
- MODIFY `AGENTS.md` (add "Parallel execution is standard" subsection under an existing top-level section)
- MODIFY `docs/guides/agent-worktree-patterns.md` (add cross-reference to the auto-dispatch path; bump Last Updated)
- (VERIFY ONLY, no edit) `docs/reference/mcp-tools.md` — auto-generated; confirm the `plan_parallelization` entry survives `generate-docs`

**Not touched:** any `agents/skills/**/SKILL.md`, any `packages/**` source, `docs/reference/mcp-tools.md` (hand-edit forbidden), lint/arch baselines.

## Skills

No design/framework skills apply — this is a docs-only phase. The generated `SKILLS.md` recommends TypeScript/testing skills (`ts-zod-integration`, `ts-testing-types`, etc.) that are irrelevant to prose ADRs. No annotations.

## Skeleton

_Not produced — task count (7) is below the standard-rigor threshold (8). Proceeding directly to full tasks._

## Conventions (verified this session)

- **ADR location & format:** `docs/knowledge/decisions/NNNN-<slug>.md`. Frontmatter: `number`, `title`, `date`, `status` (`accepted`), `tier` (`medium`), `source`. Required sections: **Context**, **Decision**, **Consequences**. Optional trailing "Alternatives Rejected" / "See also" sections are used by existing ADRs (see 0013, 0055). Highest existing number is **0055** → next two are **0056** and **0057**. (`docs/knowledge/decisions/README.md`, verified.)
- **Knowledge concept doc:** `docs/knowledge/<domain>/<slug>.md` with frontmatter `type: business_concept`, `domain`, `tags: [...]`; sections "What it is" / "Relationships" / "See also" (pattern from `docs/knowledge/core/health-signal-contract.md`). Ingested by the knowledge pipeline as graph nodes — creating the doc IS the graph enrichment.
- **Link-based doc-coverage:** a file counts as documented only when another `docs/` markdown links it by basename. New docs get inbound links from AGENTS.md + cross-links between the ADRs and the knowledge doc. (`mcp-tools.md` cannot be a linker — it is generated.)
- **Hooks:** `--no-verify` is blocked. Plugin regen (slow ~5-12 min) only fires when `agents/skills/**` is staged — Phase 4 stages none, so commits are fast (matches Phase-3 Task-5 docs-only commit). Pre-commit runs prettier `format:check`; write prettier-clean markdown and re-add if the hook reformats.

## Tasks

### Task 1: Write ADR 0056 — Risk-tiered non-blocking dispatch

**Depends on:** none | **Files:** `docs/knowledge/decisions/0056-risk-tiered-non-blocking-dispatch.md`

1. Create the file with this exact content:

   ```markdown
   ---
   number: 0056
   title: Risk-tiered, non-blocking parallel dispatch (announce-and-proceed)
   date: 2026-07-07
   status: accepted
   tier: medium
   source: docs/changes/standardize-parallel-execution/proposal.md
   ---

   ## Context

   The harness already owned every primitive for safe parallelism — a topological
   wave-grouper (`findParallelGroups`), severity-aware conflict prediction
   (`predict_conflicts`), and worktree isolation — but nothing composed them into the
   standard execution path, and parallelism only happened when a human typed "work in
   parallel." Making parallel dispatch _automatic_ raised the question this ADR settles:
   **when may the harness dispatch a parallel wave on its own, and when must it pause?**

   A gate on every wave would reintroduce the friction we are removing. A gate on no wave
   would dispatch work the conflict predictor flagged as risky. The right answer is
   friction _in proportion to predicted risk_, keyed off the severity that
   `predict_conflicts` already returns plus whether graph analysis was available.

   ## Decision

   The firing policy is **risk-tiered and non-blocking** — "announce-and-proceed." Before
   any dispatch the harness emits a `narration` (the standardized DAG summary) and then
   acts; it does not halt for a yes/no on the common case. The per-wave firing decision
   (`auto-dispatch` | `confirm` | `serialize`) is derived deterministically by
   `classifyFiring` (`packages/core/src/parallelization/plan.ts`):

   | Condition (evaluated in order)                   | Firing          |
   | ------------------------------------------------ | --------------- |
   | `high` conflict severity                         | `serialize`     |
   | wave size `< minWaveSize` (default 3)            | `serialize`     |
   | `medium` conflict severity                       | `confirm`       |
   | `analysisLevel: 'file-only'` (graph unavailable) | `confirm`       |
   | `none` **or** `low` severity + `graph-expanded`  | `auto-dispatch` |

   - **`auto-dispatch`** — emit the wave's narration line and dispatch immediately via
     `harness-parallel-agents` with worktree-per-unit isolation. **No stop.**
   - **`confirm`** — surface the wave + narration, take exactly one plain-text confirmation.
     Covers medium severity and the graph-unavailable case, where transitive conflicts are
     unknown and human judgment adds safety.
   - **`serialize`** — run sequentially. Covers high-severity groups (the predictor already
     regroups these) and waves too small to justify parallelism. Announce the reason.

   **Intentional extension beyond the spec's enumerated cases.** Decision 2 of the proposal
   enumerated three buckets: clean → dispatch, medium/graph-unavailable → confirm, high →
   serialize. It did not spell out **`low` severity with `graph-expanded` analysis**. This
   ADR ratifies the deliberate extension — implemented in `classifyFiring` — that treats
   `low` + `graph-expanded` the same as `none`: **`auto-dispatch`**. Rationale: with the
   graph available, low-severity conflicts are the predictor's least-uncertain signal;
   gating them would contradict "announce-and-proceed" and reintroduce friction where
   judgment adds nothing. This was Phase-2 review suggestion **SUG-3**, deferred here for an
   explicit, reviewable record rather than being buried in code. `file-only` low-severity
   still requires `confirm` (the uncertainty is the missing graph, not the severity).

   **Cross-bucket cap (P2-IMP-1).** A wave that would otherwise `auto-dispatch` but whose
   direct upstream runs in the `serialized`/`cyclic` channel (a task that is _not_ a
   parallel-safe wave) is **capped to `confirm`** — never weakened, and never forced all the
   way to `serialize`. A prerequisite dispatched through a non-wave channel means a human /
   execution gate stands between the two; the dependent wave must not silently auto-dispatch
   across that boundary. This is why dispatch must honor the wave _ordering_ (serialized and
   cyclic members first, then waves in array order) and must **not** key off the `firing`
   field alone.

   ## Consequences

   - **Autonomy where it is safe, a gate only where judgment helps.** Clean and
     low-severity graph-expanded waves proceed untouched; friction appears only for medium
     severity, missing-graph uncertainty, and cross-bucket boundaries.
   - **A referable policy.** Future skills that dispatch fan-out work (`harness-code-review`
     Phase 4, `release-readiness` Phase 2) can adopt this same tier vocabulary instead of
     improvising.
   - **Serial is always the safe default.** When in doubt — fewer than `minWaveSize`
     independent tasks, a declined `confirm`, or no graph and no confirmation — execution
     falls back to sequential, preserving today's behavior.
   - **Determinism is load-bearing.** `classifyFiring` and `narrate` derive from sorted
     inputs, so the announced plan is reproducible and reviewable.
   - **The `low` + `graph-expanded` extension is now on the record** — if a future change
     wants to gate it, this ADR is the thing it supersedes, not a silent code edit.

   ## See also

   - ADR 0057 — `dependsOn` in the plan-task schema (the edges this policy schedules over).
   - `docs/knowledge/core/parallelization-plan.md` — the `risk-tiered-dispatch` concept.
   - `docs/reference/mcp-tools.md` — the `plan_parallelization` MCP tool.
   - Spec: `docs/changes/standardize-parallel-execution/proposal.md` (Decision 2).
   ```

2. Verify prettier-clean: `pnpm exec prettier --check docs/knowledge/decisions/0056-risk-tiered-non-blocking-dispatch.md`
3. Commit: `docs(decisions): add ADR 0056 risk-tiered non-blocking dispatch`
4. Run: `harness validate` (expect baseline == 390, no new findings)

### Task 2: Write ADR 0057 — `dependsOn` in the plan-task schema

**Depends on:** none (parallelizable with Task 1 — different file) | **Files:** `docs/knowledge/decisions/0057-depends-on-plan-task-schema.md`

1. Create the file with this exact content:

   ```markdown
   ---
   number: 0057
   title: dependsOn is the explicit dependency edge in the plan-task schema
   date: 2026-07-07
   status: accepted
   tier: medium
   source: docs/changes/standardize-parallel-execution/proposal.md
   ---

   ## Context

   Standardizing parallel execution needs an explicit, machine-readable dependency edge
   between plan tasks. Before this change no such field existed anywhere: the improvised
   "Phase 1 blocks 2 & 3" reasoning was recomputed by hand each session, and the only
   independence signal was a task's `files` list (overlap ⇒ conflict). `files` captures
   _implicit_ conflict edges but cannot express a _pure ordering_ dependency (task B must
   follow task A even when they touch different files). That missing edge is the one
   genuinely absent data-model piece.

   A related field, `owns:[paths]` (roadmap #601), was considered. It is a richer ownership
   declaration but is out of scope for this feature — adopting its _authoring_ would pull in
   #601 wholesale.

   ## Decision

   Add an optional **`dependsOn?: string[]`** (task ids) to the `PlanTask` type
   (`@harness-engineering/types`). It is the explicit dependency edge; `files` remains the
   independence-checking input. `buildTaskGraph`
   (`packages/core/src/parallelization/plan.ts`) builds the DAG consumed by
   `findParallelGroups` as **explicit `dependsOn` edges unioned with implicit file/`owns`
   overlap edges**, oriented earlier-declared → later-declared for determinism.

   **Relationship to `owns:[paths]` (#601): consumed-if-present, not owned here.**
   `footprintOf` unions a task's `files` and `owns` when computing overlap, so a plan that
   _already_ carries `owns` gets richer conflict edges for free. But this feature does **not**
   define, author, or validate `owns` — that field and its authoring workflow stay roadmap
   #601. Phase 4 only ratifies the read-if-present seam.

   **Validation** (`validatePlanTasks`) enforces the contract:

   - `dependsOn` referencing an **unknown task id** → **hard error**.
   - A **dependency cycle** → **hard error**. Cycles are detected over the _same_ combined
     graph the planner schedules (explicit `dependsOn` ∪ file/`owns` overlap), so validation
     and `planParallelization` agree on what a cycle is — a set the planner drops into
     `cyclic` is never validated as clean.
   - A task depending on a task declared **later** in the plan (consumer before producer) →
     **warning** (the plan lists them out of natural order; still schedulable).

   ## Consequences

   - **A durable contract other tooling builds on.** `plan_parallelization`, the soundness
     reviewer, and future scheduling tools read one canonical edge instead of re-deriving it.
   - **Backwards compatible.** `dependsOn` is optional; plans without it behave exactly as
     before (independence inferred from `files` overlap alone).
   - **Cycles fail loud and early.** A cyclic plan is rejected at validation, not discovered
     mid-dispatch.
   - **The `owns` seam is preserved without commitment.** #601 can later own `owns` authoring
     without renegotiating this contract; consumers already tolerate its presence.
   - **`files` keeps its meaning.** It stays the independence/conflict input; `dependsOn` adds
     ordering the file list cannot express.

   ## See also

   - ADR 0056 — risk-tiered non-blocking dispatch (schedules over these edges).
   - `docs/knowledge/core/parallelization-plan.md` — the `task-dependency-dag` concept.
   - Roadmap #601 — `owns:[paths]` authoring (the field consumed-if-present here).
   - Spec: `docs/changes/standardize-parallel-execution/proposal.md` (Decision 4).
   ```

2. Verify prettier-clean: `pnpm exec prettier --check docs/knowledge/decisions/0057-depends-on-plan-task-schema.md`
3. Commit: `docs(decisions): add ADR 0057 dependsOn plan-task schema`
4. Run: `harness validate` (expect baseline)

### Task 3: Write the knowledge concept doc (graph enrichment)

**Depends on:** Task 1, Task 2 (its "See also" links both ADRs by basename) | **Files:** `docs/knowledge/core/parallelization-plan.md` | **Category:** integration (Knowledge Impact)

1. Create the file with this exact content:

   ```markdown
   ---
   type: business_concept
   domain: core
   tags: [parallelization-plan, task-dependency-dag, risk-tiered-dispatch, waves, firing, autopilot]
   ---

   # Parallelization plan

   Three related concepts introduced by the standardize-parallel-execution feature,
   materialized here so the knowledge pipeline ingests them as graph nodes.

   ## What it is

   **`parallelization-plan`** — the `ParallelizationPlan` produced by `planParallelization`
   (`packages/core/src/parallelization/plan.ts`) and the `plan_parallelization` MCP tool. It
   partitions a phase's tasks into three mutually disjoint dispatch channels — `waves`
   (parallel-safe, each annotated with severity + firing), `serialized` (forced sequential:
   high-severity group members or cycle members), and `cyclic` (blocked: dependency cycles)
   — plus a human-readable `narration` for announce-and-proceed.

   **`task-dependency-dag`** — the directed acyclic graph `buildTaskGraph` derives from plan
   tasks: explicit `dependsOn` edges unioned with implicit file/`owns` overlap edges,
   wave-grouped by the existing `findParallelGroups` (Kahn topological sort). Cycles are
   surfaced, not scheduled. See ADR 0057.

   **`risk-tiered-dispatch`** — the non-blocking firing policy (`auto-dispatch` | `confirm` |
   `serialize`) `classifyFiring` assigns per wave, keyed off conflict severity and
   `analysisLevel`, with a cross-bucket cap. See ADR 0056.

   ## Relationships

   - `harness-autopilot` (EXECUTE) and standalone `harness-execution` **consume**
     `plan_parallelization` as a pre-dispatch step.
   - `plan_parallelization` **wraps** `findParallelGroups` (wave grouping) and
     `predict_conflicts` (severity) — no scheduling logic is duplicated across skills.
   - `harness-planning` **records** the `dependsOn` edges the DAG is built from.
   - `harness-parallel-agents` is **invoked by** autopilot to dispatch a wave with
     worktree-per-unit isolation (`docs/guides/agent-worktree-patterns.md`).

   ## See also

   - ADR 0056 — risk-tiered non-blocking dispatch (`0056-risk-tiered-non-blocking-dispatch.md`).
   - ADR 0057 — `dependsOn` plan-task schema (`0057-depends-on-plan-task-schema.md`).
   - `docs/reference/mcp-tools.md` — the `plan_parallelization` tool.
   - `docs/knowledge/architecture/graph-schema.md` — graph node vocabulary.
   - Spec: `docs/changes/standardize-parallel-execution/proposal.md` (Knowledge Impact).
   ```

2. Verify prettier-clean: `pnpm exec prettier --check docs/knowledge/core/parallelization-plan.md`
3. Commit: `docs(knowledge): record parallelization-plan graph concepts`
4. Run: `harness validate` (expect baseline)

### Task 4: Add the "parallel execution is standard" section to AGENTS.md

**Depends on:** Task 1, Task 2, Task 3 (section links all three new docs + the MCP tool, giving them inbound basename links) | **Files:** `AGENTS.md`

1. Read `AGENTS.md` around the `## Key Concepts` (line ~407) and `## Development Workflow` (line ~296) headings to pick the correct insertion point (a new `### Parallel execution is standard` subsection under `## Key Concepts`, immediately before the next `### ` sibling — Read to confirm the exact anchor line before editing).
2. Insert this subsection (adjust the surrounding blank lines to match the file's existing spacing):

   ```markdown
   ### Parallel execution is standard

   Parallel execution is now a **standard, automatic** part of the build loop — not a mode a
   human has to ask for. During execution the harness derives the task dependency DAG from
   each task's `dependsOn` edges and `files`, decides the maximum _safe_ parallelism, and
   dispatches it **without being asked**, while keeping the human gate exactly where predicted
   risk warrants it (announce-and-proceed).

   - **How it decides:** the `plan_parallelization` MCP tool (core module
     `packages/core/src/parallelization/plan.ts`) wraps `findParallelGroups` +
     `predict_conflicts` to produce a `ParallelizationPlan` — parallel `waves`, `serialized`
     and `cyclic` channels, and a `narration`. Each wave gets a risk-tiered firing decision:
     `auto-dispatch` (clean / low-severity with graph) proceeds with no stop; `confirm`
     (medium severity, or graph unavailable) takes one confirmation; `serialize` (high
     severity, or too few tasks) runs sequentially. See **ADR 0056**
     (`docs/knowledge/decisions/0056-risk-tiered-non-blocking-dispatch.md`).
   - **The data model:** tasks carry an optional `dependsOn: string[]` — the explicit
     dependency edge — validated for cycles and unknown ids. See **ADR 0057**
     (`docs/knowledge/decisions/0057-depends-on-plan-task-schema.md`).
   - **The skills:** `harness-planning` records `dependsOn`; `harness-autopilot` (EXECUTE) and
     `harness-execution` call `plan_parallelization` pre-dispatch; `harness-parallel-agents`
     dispatches each wave with worktree-per-unit isolation
     (`docs/guides/agent-worktree-patterns.md`).
   - **Concepts:** `docs/knowledge/core/parallelization-plan.md`.
   - **Serial is still the safe default:** fewer than `minWaveSize` (default 3) independent
     tasks, a declined `confirm`, or no graph + no confirmation all fall back to sequential.
   ```

3. Verify prettier-clean: `pnpm exec prettier --check AGENTS.md`
4. Commit: `docs(agents): declare parallel execution a standard part of the build loop`
5. Run: `harness validate` (expect baseline)

### Task 5: Cross-reference the auto-dispatch path from the worktree guide

**Depends on:** Task 4 | **Files:** `docs/guides/agent-worktree-patterns.md` | **Category:** integration (Documentation Updates)

1. Read `docs/guides/agent-worktree-patterns.md` `## Parallel Agent Work` (line ~149) and the footer (line ~170).
2. Append a short cross-reference block at the end of the `## Parallel Agent Work` section (before the `## Connection to Harness Engineering Principles` heading):

   ```markdown
   ### Automatic dispatch uses this pattern

   This worktree-per-unit model is what the harness dispatches **automatically** during
   parallel execution. When `harness-autopilot` (EXECUTE) / `harness-execution` call the
   `plan_parallelization` MCP tool and a wave is cleared for `auto-dispatch`, each task in the
   wave runs in its own worktree per this guide, with sequential commits and squash-merge at
   integration. The firing policy that decides _whether_ a wave auto-dispatches is **ADR 0056**
   (`docs/knowledge/decisions/0056-risk-tiered-non-blocking-dispatch.md`); AGENTS.md
   ("Parallel execution is standard") is the overview.
   ```

3. Bump the footer: change `_Last Updated: 2026-03-16_` to `_Last Updated: 2026-07-07_`.
4. Verify prettier-clean: `pnpm exec prettier --check docs/guides/agent-worktree-patterns.md`
5. Commit: `docs(guides): cross-reference auto-dispatch from worktree patterns`
6. Run: `harness validate` (expect baseline)

### Task 6: Coherence read across all Phase-4 docs

**Depends on:** Task 1-5 | **Files:** none (read-only gate) | `[checkpoint:human-verify]`

1. Read all five touched/created files end-to-end and confirm:
   - Firing vocabulary is EXACTLY `auto-dispatch` / `confirm` / `serialize`; channels EXACTLY `waves` / `serialized` / `cyclic`; announcement field EXACTLY `narration`. No invented terms.
   - The `low` + `graph-expanded` → `auto-dispatch` extension (SUG-3) is stated as **intentional** in ADR 0056 and nowhere contradicted.
   - The cross-bucket cap is described as auto-dispatch → **confirm** (not serialize) consistently.
   - ADR 0057 says `owns` is **consumed-if-present, not owned** (stays #601) — consistent across ADR, knowledge doc, and AGENTS.md.
   - Every new doc has an inbound basename link: run `grep -rl "0056-risk-tiered-non-blocking-dispatch\|0057-depends-on-plan-task-schema\|parallelization-plan.md" docs AGENTS.md --include=*.md` and confirm each new file's basename appears in at least one OTHER file.
   - Numbering: `0056`/`0057` are unique (`ls docs/knowledge/decisions/005[67]*`).
2. `[checkpoint:human-verify]` — Present the coherence findings and the inbound-link grep results. Wait for confirmation before the final verification task. (If run under orchestrator non-blocking mode, capture evidence and proceed, per Phase-3 precedent.)

### Task 7: Regen-checks, lint, and validate

**Depends on:** Task 6 | **Files:** none (verification only) | **Category:** integration (Registrations / drift)

1. `pnpm generate-docs --check` — expect **exit 0**. This proves `docs/reference/mcp-tools.md` (auto-generated) did NOT drift — the Phase-3 `plan_parallelization` entry is intact and nothing Phase 4 wrote needs regeneration. If it exits non-zero, a generated doc drifted; STOP and investigate (do not blindly commit).
2. `pnpm generate:plugin:check` — expect **exit 0**. Phase 4 edited no `SKILL.md`, so no plugin artifact should change. Non-zero ⇒ unexpected skill drift; STOP.
3. `pnpm format:check` — expect pass on all new/edited markdown (or run `pnpm format` and re-commit the reformatted files under `docs/` + `AGENTS.md` only).
4. `harness validate` — expect the pre-existing baseline (390 issues); confirm no NEW findings reference the Phase-4 files (`0056-*`, `0057-*`, `parallelization-plan.md`, `AGENTS.md`, `agent-worktree-patterns.md`).
5. If steps 1-3 produced any staged regenerated/reformatted artifact, commit: `chore(docs): regen/format checks for phase-4 docs`. Otherwise no commit (verification-only).

## Sequence & Parallelism

- **Task 1 ∥ Task 2** — independent files (both ADRs); safe to write concurrently.
- **Task 3** after 1 & 2 (links both ADR basenames).
- **Task 4** after 1-3 (section links all three new docs).
- **Task 5** after 4 (references AGENTS.md section).
- **Task 6** (coherence gate) after 1-5.
- **Task 7** (verification) after 6.

Estimated total: 7 tasks, ~28 minutes.

## Uncertainties

- [ASSUMPTION] AGENTS.md `## Key Concepts` is the right home for the new subsection. Task 4 Read-confirms the anchor before editing; if a more fitting section exists (e.g. `## Development Workflow`), place it there — either satisfies Observable Truth 3.
- [ASSUMPTION] `docs/knowledge/core/` (domain `core`) is the correct home for the concept doc, since `planParallelization` lives in `@harness-engineering/core`. If the pipeline expects a different domain, the frontmatter `domain` is the only field to change.
- [DEFERRABLE] Exact prose wording of ADR Consequences — refined during writing without changing the plan.
- [RESOLVED] ADR numbers `0056`/`0057` are free (highest existing is `0055`; duplicate historical numbers exist below but do not affect the next sequential number).

## Concerns (for handoff / FINAL_REVIEW / push)

1. **arch:fail on Phase-2 `plan.ts` complexity vs un-bumped baseline (informational, carried from Phase 3).** Pre-commit `harness ci check` prints `arch: fail` on the **pre-existing** Phase-2 module (`functionLength 68>50`; `complexity 234>233`; `module-size 178135>177647`; `dep-depth 515>501` vs the committed baseline). The hook **proceeds** past it, so commits land. The baseline was **deliberately not bumped** (Phase-2 handoff + orchestrator "revert baseline drift"). **Phase 4 must NOT bump baselines** — it is docs-only and touches no source. **Recommendation for FINAL_REVIEW/push:** this is a lint-baseline hygiene item, **not** an architectural decision — it does **not** warrant an ADR. The **PR description** should note the deferred baseline bump (owed by Phase 2) so a reviewer decides whether to bump in a separate, source-scoped commit; if the gate ever becomes fail-closed, that bump becomes required. Do not entangle it with the Phase-4 docs commits.
2. **Changeset (docs-only).** This phase produces no shippable package change. At PR time it likely needs an **empty / no-release changeset** to pass the changeset gate. Beware the known prettier-vs-empty-changeset conflict (the `---\n\n---` no-release marker gets collapsed by prettier and rejected — add the marker file to `.prettierignore` if used). Decided at push time, not planning.
3. **`docs/reference/mcp-tools.md` is auto-generated (do-not-edit).** Item 3 of the phase scope ("ensure the reference index lists the new capability") is satisfied already (Phase 3) and is **verification-only** here (Task 7 step 1). Any temptation to hand-add cross-links to that file must be resisted — `generate-docs` would overwrite them and the `--check` gate would flag drift.
4. **`.harness/security/timeline.json` side-effect drift.** Some hook runs regenerate it (already dirty in the working tree at session start). Revert after commits; do not stage it (matches the standing "revert side-effect drift" instruction).
5. **No SKILL.md edits in Phase 4** — so the slow four-platform plugin regen (~5-12 min) should NOT trigger; commits stay fast. If a commit unexpectedly triggers it, `agents/skills/**` was staged by mistake — unstage and re-scope.
