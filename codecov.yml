coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
        # Coverage regressions block the PR — not just a warning
        if_ci_failed: error
    patch:
      default:
        target: 80%
        if_ci_failed: error
  flags:
    unittests:
      paths:
        - packages/
