packages:
  - 'packages/*'
  - 'docs'
  - 'agents/skills'

autoInstallPeers: true
strictPeerDependencies: false

# Overrides moved to package.json `pnpm.overrides` — pnpm 8.x reads that field
# but not the `overrides` key in this workspace file. Removing the stale block
# also clears confusion about which list is authoritative.
